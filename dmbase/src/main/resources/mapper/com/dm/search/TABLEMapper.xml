<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dm.search.sqldao.DatabaseMapper">
	<select id="selectTables" resultType="java.util.HashMap">
		SELECT TABLE_NAME as
		tableName,TABLE_COMMENT as description FROM
		INFORMATION_SCHEMA.TABLES
		WHERE TABLE_SCHEMA =#{database}
	</select>
	<select id="selectColumns" resultType="java.util.HashMap">
		select column_name
		name,COLUMN_TYPE type,COLUMN_COMMENT description from
		information_schema.COLUMNS where TABLE_SCHEMA =#{database} and
		TABLE_NAME=#{tableName}
	</select>
</mapper>
